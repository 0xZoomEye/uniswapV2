{
  "name": "uniswapv2",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "hardhat clean",
    "prefix": "npm run compile",
    "fix": "node scripts/fix.js",
    "precompile": "npm run clean",
    "compile": "hardhat compile",
    "deploy": "HARDHAT_NETWORK=localhost node scripts/deploy.js",
    "swap": "HARDHAT_NETWORK=localhost node scripts/swap.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^3.4.1",
    "@uniswap/lib": "4.0.1-alpha",
    "@uniswap/v2-core": "1.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.12"
  }
}
